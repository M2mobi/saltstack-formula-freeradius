# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: freeradius
  rootgroup: root
  config_dir: /etc/freeradius
  user: freeradius
  group: freeradius
  service:
    name: freeradius
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
